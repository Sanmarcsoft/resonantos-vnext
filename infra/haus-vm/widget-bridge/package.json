{
  "name": "@resonantos/haus-widget-bridge",
  "version": "0.1.0",
  "description": "Drop-in HTTP shim that translates the legacy haus.matthewstevens.org widget chat contract into Hermes Agent profile invocations.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --target=bun --outfile=dist/server.js",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.0"
  }
}
